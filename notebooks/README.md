# Notebooks

## Databricks

Use `notebooks/databricks_demo.ipynb` to run the project inside a Databricks workspace repo checkout.

What it demonstrates:
- single query run (router → retriever → answer)
- offline evaluation + router weight update
- inspection of the SQLite telemetry DB


